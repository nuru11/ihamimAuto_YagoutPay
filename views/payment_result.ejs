<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body { background-color: #e8f9f3; color: #060452; font-family: Arial, sans-serif; }
    .success-box {
      max-width: 650px;
      margin: 80px auto;
      padding: 30px;
      background: white;
      border-radius: 10px;
      border: 2px solid #28a745;
      box-shadow: 0px 4px 15px rgba(0,0,0,0.1);
    }
    h1 { color: #28a745; font-weight: bold; }
    pre {
      background: #f8f9fa;
      padding: 15px;
      border-radius: 8px;
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="success-box">
    <h1>✅ Payment Successful!</h1>
    <p>Your transaction was processed successfully.</p>

    <h5> Transaction Response:</h5>
    <pre><%= rawResponse %></pre>

    <a href="/transaction" class="btn btn-primary mt-3">Back to Transaction</a>
  </div>
</body>
</html>
