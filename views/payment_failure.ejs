<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment Failed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body { background-color: #fff3f3; color: #060452; font-family: Arial, sans-serif; }
    .failure-box {
      max-width: 500px;
      margin: 80px auto;
      padding: 30px;
      background: white;
      border-radius: 10px;
      text-align: center;
      border: 2px solid #dc3545;
    }
    h1 { color: #dc3545; }
  </style>
</head>
<body>
  <div class="failure-box">
    <h1>❌ Payment Failed</h1>
    <p><%= error %></p>
    <% if (typeof response !== 'undefined') { %>
      <h5>Transaction Response:</h5>
      <pre><%= response %></pre>
    <% } %>
    <a href="/" class="btn btn-danger mt-3">Try Again</a>
  </div>
</body>
</html>
